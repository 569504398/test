export default {
            change(state,num){
                state.tabController=num
            },
            backTopStatus(state,statu){
                  state.backTopStatus=statu
            }
}